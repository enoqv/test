# Claude Code plugins to install on devcontainer startup.
# One `plugin-name@marketplace` spec per line.
# Lines starting with # are ignored.

# Upstash Context7 — docs-aware tooling (skill + /context7:docs command).
context7-plugin@context7-marketplace

# obra/superpowers — TDD, brainstorm, write-plan, execute-plan skills.
superpowers@superpowers-marketplace
