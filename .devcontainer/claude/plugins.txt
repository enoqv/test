# Claude Code plugins to install on devcontainer startup.
# One `plugin-name@marketplace` spec per line. Comments (#) ignored.
# The official marketplace is `claude-plugins-official` and is always
# available without a separate `marketplace add`.

# Upstash Context7 — docs-aware lookup (skill + agent + /context7:* cmds).
context7@claude-plugins-official

# obra/Superpowers — brainstorm, plan, TDD, subagent-driven dev workflow.
superpowers@claude-plugins-official
