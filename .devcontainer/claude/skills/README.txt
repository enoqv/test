Drop Claude Code skills here. Each skill is its own directory:

  .devcontainer/claude/skills/<skill-name>/SKILL.md

The claude-setup.sh script copies everything under this directory into
$CLAUDE_CONFIG_DIR/skills/ on container start.
